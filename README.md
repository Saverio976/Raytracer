# this branch is only for caracursed purpose

```bash
cmake .
make
./raytracer --scene-path ./tests/testSceneXav.yaax --output-path testit
```

This will output a single `0.ppm` file with the cursed image
